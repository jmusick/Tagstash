# IMPORTANT: Copy this file to .env and update with your actual values

# Frontend configuration
# Optional API base URL override for the browser app.
# Leave unset for local development to use Vite's /api proxy.
VITE_API_URL=

# API/runtime secrets for Cloudflare Pages Functions live in .dev.vars.
# Copy .dev.vars.example -> .dev.vars and fill in your values.
